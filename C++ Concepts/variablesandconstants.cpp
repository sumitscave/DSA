#include <bits/stdc++.h>
using namespace std;
int main()
{
    // cout<<"Welcome again"; comments
    // Only write comments where you have mentioned complex algorithms

    /*
    cout << "Welcome again";
    cout << "Welcome again"; Multi Line comment*/

    cout << "Hello World These are data types";
    cout << endl;
    cout << endl;

    int score; // variable declared
    score = 100; // initialise

    const int uid = 232323; //won't change as Its constant now
    // uid = 2333; wont change
    int newScore = 1100; //both of them together
    int hiteshBalance = 500;

    // identifier = already defined special keyword which we cant use in variable
    // only use name which makes sense.

    return 0;
}